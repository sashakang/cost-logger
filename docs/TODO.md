# IN PROGRESS
[ ] When a category is assigned, look for another unopened notification, not for the next one, because the one we're looking for could be the previous one.

# BACKLOG  
[ ] RE: When a user opens a notification to assign a category after the user assigns a category to the row open next notification to categorize. Add Approve button to the screen to confirm currently assigned category. UPD: When user selects a category it assigns it to the row right away. The app does not require user to click Approve button.
[ ] Add Approve button to the Category assignment screen. It only makes it look for the next notification to categorize after the user clicks Approve button.

# DONE
[x] Add timestamp UTC to the left.  
[x] Add qualification to the interface.  
[x] Force re-read all open notifications in case some of them were missed.
[x] Allow for negative amounts.  
[x] And documentation screen.  
[x] When adding a row to the sheet copy cells from G1:L1 to the new row. Then use the value from I column in the notification on the phone.
[x] When a user opens a notification to assign a category after the user assigns a category to the row open next notification to categorize. Add Approve button to the screen to confirm currently assigned category.
[x] RE: When adding a row to the sheet copy cells from G1:L1 to the new row. Then use the value from I column in the notification on the phone. UPD: Use G2:L2 as a template for the new row.
[x] When opening an assign category screen show the category assigned automatically in the category column of the transactions sheet. Highlight it as a selected category on the list of the buttons of the categories.
[x] Add Comment to the transaction columns and to the Category assignment screen and to the Manual entry screen.
